apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: bgd
  namespace: demo
  annotations:
    kubernetes.io/ingress.class: addon-http-application-routing
spec:
  rules:
  - host: bgd.84b92564d8a04b128b60.uksouth.aksapp.io
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: bgd
            port:
              number: 8080